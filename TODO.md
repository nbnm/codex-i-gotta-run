# TODO

## Next Tasks

- Add a new CLI mode where messages are forwarded to a Telegram bot.
- Send all messages through the bot to a Telegram user.
- For messages that require approval, have the bot explicitly mention the user with `@user_id` and present Telegram commands or buttons for actions such as approve and deny.
- For the last message in a turn, when Codex finishes the task, have the bot explicitly mention the user with `@user_id`.
- Add Telegram support for several threads at the same time.
